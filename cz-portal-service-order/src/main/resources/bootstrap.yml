spring:
  application:
    name: cz-portal-service-order
  output:
      ansi:
        enabled: always
server:
  port: 9101
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka