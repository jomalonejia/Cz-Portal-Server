spring:
  application:
    name: cz-portal-security
  output:
      ansi:
        enabled: always
server:
  port: 9100
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
