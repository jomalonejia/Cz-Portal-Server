spring:
  application:
    name: cz-portal-service-user
  output:
      ansi:
        enabled: always
server:
  port: 9103
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka