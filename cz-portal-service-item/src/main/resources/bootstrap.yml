spring:
  application:
    name: cz-portal-service-item
  output:
      ansi:
        enabled: always
server:
  port: 9102
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka